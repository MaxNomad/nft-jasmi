/**
 * @desc Routes for getting cards deck.
 * @const
 * @type {string}
 * @default
 */

/**
 * @desc Routes for the whole app.
 * @const
 * @type {object}
 * @default
 */
export const ROUTES = {
  root: "/",
  item: "/item",
  collection: "/collection"
};
